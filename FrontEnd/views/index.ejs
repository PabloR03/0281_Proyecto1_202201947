<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Monitor del Sistema - CPU y RAM</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>🖥️ Monitor del Sistema</h1>
        <p class="subtitle">Monitoreo en tiempo real de CPU y RAM</p>
        
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error">
                <h3>❌ Error de Conexión</h3>
                <p><%= error %></p>
                <button onclick="recargarDatos()">🔄 Reintentar</button>
            </div>
        <% } else if (metrics) { %>
            <div class="status-bar">
                <span class="status-item">
                    <span class="status-dot online"></span>
                    Backend: Conectado
                </span>
                <span class="status-item">
                    📅 Última actualización: <%= metrics.timestamp %>
                </span>
                <span class="status-item">
                    ⚡ Estado: <%= metrics.health %>
                </span>
            </div>

            <div class="metrics-container">
                <div class="metric-card cpu-card">
                    <div class="metric-header">
                        <h2>🖥️ CPU</h2>
                        <button onclick="actualizarCPU()" class="refresh-btn">🔄</button>
                    </div>
                    <div class="metric-content">
                        <div id="cpu-data" class="metric-data">
                            <%= formatMetricOutput(metrics.cpu, 'CPU') %>
                        </div>
                    </div>
                </div>

                <div class="metric-card ram-card">
                    <div class="metric-header">
                        <h2>💾 RAM</h2>
                        <button onclick="actualizarRAM()" class="refresh-btn">🔄</button>
                    </div>
                    <div class="metric-content">
                        <div id="ram-data" class="metric-data">
                            <%= formatMetricOutput(metrics.ram, 'RAM') %>
                        </div>
                    </div>
                </div>
            </div>

            <div class="controls">
                <button onclick="actualizarTodo()" class="primary-btn">🔄 Actualizar Todo</button>
                <button onclick="toggleAutoRefresh()" id="auto-refresh-btn" class="secondary-btn">
                    ⏸️ Pausar Auto-actualización
                </button>
            </div>

            <div class="info-panel">
                <h3>ℹ️ Información</h3>
                <p>Los datos se actualizan automáticamente cada 10 segundos.</p>
                <p>El backend actualiza las métricas cada 5 segundos.</p>
            </div>
        <% } else { %>
            <div class="loading">
                <div class="spinner"></div>
                <p>Cargando métricas del sistema...</p>
            </div>
        <% } %>
    </div>

    <script src="/js/main.js"></script>
</body>
</html>